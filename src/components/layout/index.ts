import { withInstall } from '../../utils/withInstall'

import Header from './Header'
export const ConsoleHeader = withInstall(Header)

import Logo from './Logo'
export const ConsoleLogo = withInstall(Logo)

import Navbar from './Navbar'
export const ConsoleNavbar = withInstall(Navbar)
